# ============================================================
#  VelocityLogs-Spigot v2.0 - backend config
#  Author: muvixo
# ============================================================

# Plugin messaging channel. MUST match the Velocity side.
channel: "velocitylogs:main"

# Name of this backend server.
# Leave empty to auto-detect.
server-name: ""

# Send commands to the proxy even for OP players?
log-ops: true

# Send OP status to the proxy on player join AND with every command?
report-op-status: true

# Also report OP status periodically (safety net).
op-status-interval-minutes: 5

# Hide these commands from being forwarded.
blacklist:
  - "login"
  - "register"
  - "changepassword"

# Ignore commands from these players entirely.
ignored-players: []
